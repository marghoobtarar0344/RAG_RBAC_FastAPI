version: "3.8"

services:
  fastapi-app:
    build: .
    container_name: rag-rbac-container
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}  # Load API key from .env
    volumes:
      - .:/app
